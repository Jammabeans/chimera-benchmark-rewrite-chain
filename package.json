{
  "name": "chimera-benchmark-rewrite-chain",
  "version": "0.1.0",
  "private": true,
  "description": "External Chimera benchmark scaffold for rewrite-chain weakness category.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
